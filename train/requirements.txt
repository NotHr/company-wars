# Training dependencies (install in Colab / HF Jobs)
# pip install -r train/requirements.txt

# Core training stack
torch>=2.1.0
transformers>=4.45.0
trl>=0.12.0
peft>=0.13.0
accelerate>=0.34.0
datasets>=3.0.0

# Unsloth (memory-efficient 4-bit LoRA — comment out if unavailable)
unsloth[colab-new]>=2024.11.7

# Monitoring
wandb>=0.18.0
matplotlib>=3.8.0

# GRPO needs bitsandbytes for quantization fallback
bitsandbytes>=0.44.0
